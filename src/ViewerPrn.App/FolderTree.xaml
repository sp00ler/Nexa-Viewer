<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="ViewerPrn.App.FolderTree"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ViewerPrn.App">

    <!--
        Data-bound mode throughout: ItemsSource, SelectedItem and a two-way IsExpanded on the
        container. RootNodes and SelectedNode belong to the other, node-based mode; mixing the
        two corrupts the control (DECISION-0034).
    -->
    <TreeView
        x:Name="Tree"
        SelectionMode="Single"
        Expanding="OnExpanding"
        ItemInvoked="OnItemInvoked">
        <TreeView.ItemTemplate>
            <DataTemplate x:DataType="local:FolderNode">
                <TreeViewItem
                    ItemsSource="{x:Bind Children}"
                    IsExpanded="{x:Bind IsExpanded, Mode=TwoWay}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon FontFamily="Segoe Fluent Icons" FontSize="14" Glyph="{x:Bind Glyph}" />
                        <TextBlock Text="{x:Bind Name}" TextTrimming="CharacterEllipsis" />
                    </StackPanel>
                </TreeViewItem>
            </DataTemplate>
        </TreeView.ItemTemplate>
    </TreeView>
</UserControl>
